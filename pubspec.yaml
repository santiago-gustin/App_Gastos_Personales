name: gastos_personales_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  charts_flutter: ^0.12.0
  firebase_core: ^2.29.0
  flutter:
    sdk: flutter

dev_dependencies:
  change_app_package_name: ^1.1.0

  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
